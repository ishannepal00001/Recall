# Copy to .env and fill values
# Only VITE_ prefixed vars are exposed to src/ (Vite requirement)

# Backend URL used by src/hooks/api.ts
VITE_API_URL=http://localhost:8787

# Example extensible vars - add as needed (must start with VITE_)
# VITE_APP_NAME=Recall
# VITE_SENTRY_DSN=
